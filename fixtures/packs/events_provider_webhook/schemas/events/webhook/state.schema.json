{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Webhook events provider state",
  "type": "object",
  "description": "Minimal state snapshot for webhook provider (receipt tracking only).",
  "properties": {
    "last_receipt_id": {
      "type": "string",
      "description": "Last deterministic receipt id returned by publish."
    }
  },
  "additionalProperties": true
}
