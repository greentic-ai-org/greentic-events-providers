{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Dummy events provider config",
  "type": "object",
  "description": "Configuration for the deterministic dummy events provider.",
  "properties": {
    "name": {
      "type": "string",
      "description": "Optional label for the dummy provider instance."
    },
    "echo": {
      "type": "boolean",
      "description": "When true, return the inbound payload under an `echo` field."
    }
  },
  "additionalProperties": true
}
