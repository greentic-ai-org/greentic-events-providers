{
  "requirements": {
    "config_keys": [
      "webhook.path",
      "webhook.topic_prefix"
    ],
    "secret_keys": [
      "WEBHOOK_SIGNING_SECRET"
    ],
    "webhook_required": true,
    "subscriptions_required": false
  }
}
