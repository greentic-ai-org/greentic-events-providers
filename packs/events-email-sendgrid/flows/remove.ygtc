id: remove
title: Remove
description: Remove lifecycle flow.
type: messaging
start: teardown

nodes:
  teardown:
    templating.handlebars:
      text: |
        Remove events-email-sendgrid resources and cleanup configuration.
        Revoke runtime dependencies and subscriptions if present.
    routing:
      - out: true
