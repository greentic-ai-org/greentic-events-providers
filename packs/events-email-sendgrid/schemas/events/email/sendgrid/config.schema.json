{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "events.email.sendgrid provider config",
  "type": "object",
  "required": ["messaging_provider_id"],
  "properties": {
    "messaging_provider_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable provider id used for routing and persistence keys."
    },
    "from": {
      "type": "string",
      "description": "Optional default From address for outbound integrations."
    },
    "persistence_key_prefix": {
      "type": "string",
      "description": "Override prefix for persisted inbound requests."
    }
  },
  "additionalProperties": false
}
